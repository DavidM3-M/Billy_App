<FrameLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".View.IngresosFragment"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="bottom"
        android:layout_margin="16dp"
        android:layout_gravity="bottom"> <!-- Asegura que queden abajo -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAgregarIngreso"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Agregar ingreso"
            android:textColor="#FFFFFF"
            android:backgroundTint="#00796B"
            app:cornerRadius="15dp"
            android:layout_marginBottom="8dp"/> <!-- Espacio entre botones -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnVolverInicio"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Volver al inicio"
            android:textColor="#FFFFFF"
            android:backgroundTint="#4CAF50"
            app:cornerRadius="15dp"/>
    </LinearLayout>

</FrameLayout>